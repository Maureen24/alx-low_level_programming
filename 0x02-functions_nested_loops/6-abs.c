#include "main.h"

/**
  * Description: _abs - computes the absolute value of a number
  * @n: parameter in use
  * Return: Always 0 (Success)
  */

int _abs(int n)
{
	if (n >= 0)
		return (n);
	else
		return (n * -1);
}
